<!---
This file is no longer used.
It's only here to prevent errors with legacy plugins that expect it to exists

If your Mura instance requires any custom mappings they should be placed in the ./cfapplication.cfm

this.mappings["/mymapping"] = variables.basedir & "/path/to/mymapping";
--->
